Topic data:
{facts}

Current script draft (JSON array of segments):
{draft}

The creator will send revision notes round by round. After EACH note, apply
it and return ONLY the full revised JSON array — same labels, same order;
keep everything not implicated by the notes unchanged.

First notes:
{feedback}